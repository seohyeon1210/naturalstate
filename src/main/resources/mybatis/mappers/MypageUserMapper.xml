<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="fs.four.naturalstate.mypage.dao.MypageUserDAO">

    <select id="findById" parameterType="String" resultType="fs.four.naturalstate.mypage.vo.MypageVO">
        SELECT user_id, password, confirm_password, user_name, phone, zip, address, detail_address, email, receive_email, joindate, auth
        FROM users
        WHERE user_id = #{userId}
    </select>

    <update id="updateUser" parameterType="fs.four.naturalstate.mypage.vo.MypageVO">
        UPDATE users
        SET
            password = #{password},
            confirm_password = #{confirmPassword},
            user_name = #{userName},
            phone = #{phone},
            zip = #{zip},
            address = #{address},
            detail_address = #{detailAddress},
            email = #{email},
            receive_email = #{receiveEmail}
        WHERE user_id = #{userId}
    </update>
</mapper>
